{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    {% for type, messages in app.session.flashbag.all() %}
        {% for message in messages %}
            <div class="row top-buffer">
                <div class="col-md-12">
                    <div class="alert alert-{{ type }} alert-dismissable">
                        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                        <p>{{ message }}</p>
                    </div>
                </div>
            </div>
        {% endfor %}
    {% endfor %}

    <h1>Bienvenue sur Admin Chouettos </h1>

    Vous pouvez :
    <ul>
        <li> Accéder à l'espace membres <a href="https://espace-membres.lachouettecoop.fr">ici</a></li>
        <li> Faire ou refaire votre mot de passe <a href="{{ path('fos_user_resetting_request') }}">ici</a></li>
    </ul>
</div>
{% endblock %}
